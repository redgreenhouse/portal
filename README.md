# RED Greenhouse Portal V1.53

| Cambio realizado | Archivos afectados |
|---|---|
| Los rangos de todas las imágenes del Módulo 2 se leen exclusivamente desde `config/field-map.json`; se eliminó la tabla de rangos duplicada en JavaScript | `module2-embedded.js`, `config/field-map.json` |
| Se corrigieron los rangos iniciales del logo en PORTADA y POE a `A2:G6`, para que guardar cambios de texto no deforme la imagen | `config/field-map.json` |
| Versión actualizada para evitar que GitHub Pages conserve el JavaScript anterior en caché | `config.js`, `index.html` |
